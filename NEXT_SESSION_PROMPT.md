# Next-session prompt — Summit School Services M&A screening (post-W5)

*Paste this whole file as the first message of a fresh Claude Code session opened in the project root
(the `summit-school-services-targets` repo). It is self-contained. Where this summary and the committed
docs differ, trust the docs (`RESUME.md` is authoritative); where the docs and the data on disk differ,
trust the data.*

---

## Where things stand
**W1–W5 are complete.** A ranked database of **2,646 contracted school-bus / special-needs operators
(1,126 A-list + 1,520 bench)** now carries a **W5 valuation/economics overlay** and a **per-target
implied valuation** (revenue / EBITDA / entry-EV / build-to-8× spread).

**The W5 source pivot (important context).** W5 was designed to layer in primary-source industry
research that an external vendor (BrightWave) would return into `7_research_requests/*/results/`.
**That drop never arrived** (those folders are still empty). The same underlying research was found
**already on disk** in the broader MASTER tree — the `02_Corp_Dev_Research/` data room (18,400+ artifacts
+ a 53,357-row evidence extraction) and the MCG/STI calibration filings — **plus Summit's own
proprietary bid models and live deals**, which are better-calibrated than external research for the core
unit-economics and valuation benchmarks. W5 was completed by synthesizing **those** sources, **by us**,
every figure cited. Full provenance: `5_working/inputs/market_overlay_SOURCES.md`.

**What W5 produced:**
- `5_working/inputs/market_overlay.csv` — 37 cited benchmark rows (`metric × sub_sector × scale_tier × geo`),
  ranges kept (low/point/high), small-n + disagreement flagged. Generated by
  `4_pipeline/_synthesize_market_overlay.py` (the documented derivation — edit its literals to refresh).
- `4_pipeline/_w5_overlay.py` — NEW additive scorer (`overlay_composite = w4_composite + Σ factor pts`;
  W4 baseline untouched). Factors: valuation-attractiveness, size/margin, succession, driver-market,
  outsourcing/EV-mandate, UT bonus (+ reserved contract-renewal). Plus the census-fleet-gated
  implied-valuation layer.
- `3_deliverables/master_targets_ranked_overlay.csv` (the new primary decision file) +
  `master_targets_overlay_A_top50.md`; diagnostics in `5_working/diagnostics/`.

## Step 0 — Orient
Read, in order: `README.md` (navigation) · `RESUME.md` (authoritative full state) · `4_pipeline/README.md`
(script index + the W5 run chain) · `5_working/inputs/market_overlay_SOURCES.md` (overlay provenance &
deferred items) · `7_research_requests/README.md` (why the briefs were not run in BrightWave).

## Step 0.5 — PREFLIGHT (Case B — the DB is gitignored/absent on a fresh clone)
```
ls -la 2_database/summit_targets.db 3_deliverables/master_targets_FULL.csv 5_working/inputs/market_overlay.csv
```
- **DB absent + `master_targets_FULL.csv` present (the normal fresh-clone state):** rebuild + regenerate:
  ```
  python3 4_pipeline/_build_master_view.py           # master_targets (2,646 × 91) from the committed CSV
  python3 4_pipeline/_synthesize_market_overlay.py   # market_overlay.csv (only needed if it's missing/changed)
  python3 4_pipeline/_w5_overlay.py                  # → overlay deliverables + diagnostics
  ```
  Success = no errors and `git diff 3_deliverables/` clean (the overlay reproduces byte-for-byte).
- **DB present:** just `python3 4_pipeline/_w5_overlay.py` to re-run the overlay.
- Do **not** run `_w4_score.py`/`_export_full.py` in Case B (they need the gitignored
  enrichment/fmcsa/fmcsa_census tables). Do **not** run the historical/destructive steps.

## Non-negotiable principles (carried forward)
- **We form the conclusions.** Vendor/extraction outputs are inputs to verify, not authoritative views.
- **Only `verified` figures anchor the model.** Keep ranges and disagreement; never collapse to a false
  point estimate. Flag small-n and definitional mismatches (the overlay already splits EBITDA margin into
  three metrics by definition — do not merge them).
- **Additive & non-destructive.** Do not edit `_w4_score.py`; the W4 baseline stays intact for comparison.
- Python 3 **stdlib only**; big binaries stay gitignored (`2_database/*.db`, `7_research_requests/*/sources/`).
- Repo: commit to `main`, short imperative subjects, push after milestones.

---

## WHAT'S NEXT — prioritized menu (what we can analyze / layer in)
*Pick with the user; each is independently runnable. A–B are the highest-leverage.*

**A. Night-one A-list research at scale — the biggest lever (needs explicit go; touches the live web).**
Run the `6_targets/` scaffold on the top A-list targets using the disciplined recon-brief template the
team standardized on (see `06_Summit_Target_Recon/American Transportation Systems/` in the MASTER tree
for the gold-standard format — public-source only, confidence-laddered, **no fabricated EBITDA/EV**).
Runbook `6_targets/README.md`; spec `6_targets/RESEARCH_SPEC.md`; batch via `_next_batch.py N`. Produces
per-target ownership/succession/contacts/contracts + a clean **revenue read that replaces the noisy
`da_revenue_est`** (the W5 reconciliation showed Data Axle revenue is internally implausible — ~$4K–$343K
per bus). Start with a pilot batch (e.g., top 25) and review before scaling. This is also what unlocks
outreach drafts.

**B. Deepen the overlay (sharpen the benchmarks we synthesized).**
- **Calibration filings (Brief 01 §C, currently deferred):** parse the MCG/STI/First Student 10-Ks in
  `02_Corp_Dev_Research/BrightWave Downloads/15_Summit-and-I-Squared/` for segment-revenue ÷ fleet →
  platform-scale rev/bus, to validate/refine the ~$98K/bus anchor. Needs a PDF tool (outside stdlib) —
  do the extraction in a helper, write verified rows into `_synthesize_market_overlay.py`, re-run.
- **Per-state numeric signals (currently cited ordinals):** mine the data-room state docs / `evidence_facts.csv`
  source PDFs for real driver-turnover, contracted-share, and reimbursement rates by state → replace the
  categorical `outsourcing_acceptance` / `ev_mandate_stringency` ordinals and the national-only
  `driver_turnover` with state numbers (lets driver_market_risk & outsourcing differentiate by state).
- **Run the 3 briefs for real:** the `7_research_requests/` briefs are still valid; run them (BrightWave or
  a research agent) to add primary-source breadth, then fold verified figures into the overlay.

**C. Port route-level P×Q into a per-target revenue build (Eric's explicit standardization ask).**
The implied revenue currently uses one national $/bus multiplier. The bid models build revenue bottom-up
(routes × service-days × rate + excess + charter) — porting that P×Q discipline (per
`Deal_and_Person_Notes_V1.md` §8.4) onto targets with route/district data (from night-research) gives a
higher-fidelity implied revenue than fleet × $/bus.

**D. Calibrate the overlay against the live pipeline.**
Back-test the implied-valuation bands against Summit's actual deals (Evergreen ~$30M/230 buses,
Brabe ~$17.8M floor/158, North ~$5–6M/75, Solaris): does the model's implied entry-EV bracket what
Summit actually bid/paid? Tighten `market_overlay.csv` where it doesn't. As Evergreen/Brabe close, fold
the closed multiples into the precedent set.

**E. Bullseye cuts (quick win).**
Re-slice `master_targets_ranked_overlay.csv` for leadership's stated sweet spots: the "$5–15M revenue,
SpEd-heavy, in-footprint" mom-and-pop (Eric: "I'll take those all day"), the NE3_A 150–499-bus band, and
per-state shortlists for the priority expansion states. Produces a focused outreach shortlist fast.

**F. Maintain.** The overlay is re-runnable; refresh `market_overlay.csv` as deals close or research
returns, then re-run `_synthesize_market_overlay.py && _w5_overlay.py`. Keep `RESUME.md` current.

---
*Definition of done for any follow-on: cited, verified, ranges-not-points, W4 baseline untouched,
deterministic re-run, `RESUME.md` updated, pushed.*
